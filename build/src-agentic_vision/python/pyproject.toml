[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "agentic-vision"
version = "0.3.0"
description = "Binary web cartography for AI agents"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Agentra Labs"}]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Rust",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Image Recognition",
]
keywords = ["ai", "agents", "vision", "web", "cartography", "mcp"]
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "mypy>=1.10"]

[project.urls]
Homepage = "https://github.com/agentralabs/agentic-vision"
Documentation = "https://github.com/agentralabs/agentic-vision/tree/main/docs"
Repository = "https://github.com/agentralabs/agentic-vision"

[tool.hatch.build.targets.wheel]
packages = ["src/agentic_vision"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.mypy]
python_version = "3.10"
strict = true
