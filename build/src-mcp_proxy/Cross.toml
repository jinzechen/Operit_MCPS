[build]
pre-build = [
    "apt-get update && apt-get install -y libssl-dev pkg-config perl make || true"
]

[target.aarch64-unknown-linux-musl]
pre-build = [
    "apt-get update && apt-get install -y libssl-dev pkg-config perl make || true"
]
